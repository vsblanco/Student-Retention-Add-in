{
  "name": "commands",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Office Add-in commands runtime (loaded by commands.html into a hidden long-lived runtime)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
